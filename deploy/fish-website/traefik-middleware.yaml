apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: api-strip
  labels:
    app.kubernetes.io/name: fish-website
    app.kubernetes.io/component: ingress
    app.kubernetes.io/part-of: fish-website
spec:
  stripPrefix:
    prefixes:
      - /api
